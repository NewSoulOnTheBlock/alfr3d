# Full Alfr3d install (core + all channel extras).
# Lean CLI/web install:  pip install -r requirements-core.txt
#
# Layered installs:
#   pip install -r requirements-core.txt
#   pip install -r requirements-channels-global.txt   # Telegram/Slack/Discord
#   pip install -r requirements-channels-cn.txt       # WeChat/Feishu/DingTalk + CN SDKs
#   pip install -r requirements-optional.txt          # extras if present

-r requirements-core.txt
-r requirements-channels-global.txt
-r requirements-channels-cn.txt
