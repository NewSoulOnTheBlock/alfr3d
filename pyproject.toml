[build-system]
requires = ["setuptools>=45.0"]
build-backend = "setuptools.build_meta"

[project]
name = "alfr3d"
version = "1.0.0"
description = "Alfr3d - AI Agent on WeChat and more"
requires-python = ">=3.7"
dependencies = [
    "click>=8.0",
    "requests>=2.28.2",
]

[project.scripts]
alfr3d = "cli.cli:main"

[tool.setuptools.packages.find]
include = ["cli*"]
